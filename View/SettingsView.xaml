<UserControl x:Class="ITMonitor.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="900"
             Background="Transparent"
             Loaded="UserControl_Loaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="⚙️ Sistem ve Otomasyon Ayarları" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource AppText}"/>
                <TextBlock Text="Ağ tarama periyotlarını, e-posta otomasyonunu, SMTP ve hesap şifrenizi yönetin." 
                           Foreground="{DynamicResource AppText}" Opacity="0.7" FontSize="14" Margin="0,5,0,0"/>
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="380"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">

                    <Border Background="{DynamicResource InputBackground}" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="🔍 Otomatik Ağ Taraması" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource AppText}" Margin="0,0,0,15"/>
                            <CheckBox x:Name="ChkAutoScan" Content="Otomatik Ağ Taramasını Aktifleştir" Foreground="{DynamicResource AppText}" FontSize="14" Margin="0,0,0,10"/>
                            <TextBlock Text="Tarama Aralığı (Dakika):" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtScanInterval" Text="30" Padding="8" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{DynamicResource InputBackground}" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="📅 Otomatik Raporlama" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource AppText}" Margin="0,0,0,15"/>
                            <CheckBox x:Name="ChkAutoReport" Content="Otomatik PDF Rapor Gönderimini Aktifleştir" Foreground="{DynamicResource AppText}" FontSize="14" Margin="0,0,0,15" Checked="ChkAutoReport_Checked" Unchecked="ChkAutoReport_Unchecked"/>

                            <StackPanel x:Name="SchedulePanel" IsEnabled="False">
                                <TextBlock Text="Gönderim Tipi:" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                                <ComboBox x:Name="CmbScheduleType" Margin="0,0,0,15" Padding="5" SelectionChanged="CmbScheduleType_SelectionChanged">
                                    <ComboBoxItem Content="Aralıklı (Örn: 2 saatte bir)" Tag="Interval"/>
                                    <ComboBoxItem Content="Sabit Saat (Örn: 08:00)" Tag="FixedTime"/>
                                </ComboBox>

                                <StackPanel x:Name="IntervalPanel" Visibility="Visible">
                                    <TextBlock Text="Kaç Saatte Bir Gönderilsin?" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                                    <TextBox x:Name="TxtIntervalHours" Text="1" Padding="8" Margin="0,0,0,15" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}"/>
                                </StackPanel>

                                <StackPanel x:Name="FixedTimePanel" Visibility="Collapsed">
                                    <TextBlock Text="Gönderim Saati (SS:DD):" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                                    <TextBox x:Name="TxtFixedTime" Text="17:00" Padding="8" Margin="0,0,0,15" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Background="{DynamicResource InputBackground}" CornerRadius="12" Padding="20">
                        <StackPanel>
                            <TextBlock Text="✉️ SMTP Sunucu Ayarları" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource AppText}" Margin="0,0,0,15"/>

                            <TextBlock Text="SMTP Sunucusu:" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtSmtpServer" Padding="8" Margin="0,0,0,10" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}"/>

                            <TextBlock Text="Port:" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtSmtpPort" Padding="8" Margin="0,0,0,10" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}"/>

                            <TextBlock Text="Gönderici E-Posta:" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtSenderEmail" Padding="8" Margin="0,0,0,10" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}"/>

                            <TextBlock Text="Uygulama Şifresi:" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                            <PasswordBox x:Name="TxtSenderPassword" Padding="8" Margin="0,0,0,15" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}"/>

                            <Grid Margin="0,5,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Button x:Name="BtnTestEmail" Grid.Column="0" Content="🧪 Test Et" Click="BtnTestEmail_Click" Height="40" Background="#F39C12" Foreground="White" BorderThickness="0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>

                                <Button x:Name="BtnSaveSettings" Grid.Column="2" Content="💾 Kaydet" Click="BtnSaveSettings_Click" Height="40" Background="#2ECC71" Foreground="White" BorderThickness="0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>
                        </StackPanel>
                    </Border>

                </StackPanel>

                <StackPanel Grid.Column="2">

                    <Border Background="{DynamicResource InputBackground}" CornerRadius="12" Padding="20" Margin="0,0,0,15">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="👥 E-Posta Alıcı Listesi" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource AppText}" Margin="0,0,0,15"/>

                            <Grid Grid.Row="1" Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="70"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="TxtNewEmailName" Grid.Column="0" Padding="8" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}" Text="Ad Soyad"/>
                                <TextBox x:Name="TxtNewEmailAddress" Grid.Column="2" Padding="8" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}" Text="eposta@adres.com"/>

                                <Button x:Name="BtnAddEmail" Grid.Column="4" Content="Ekle" Click="BtnAddEmail_Click" Background="#3498DB" Foreground="White" BorderThickness="0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>

                            <ListView x:Name="EmailList" Grid.Row="2" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource AppText}" MinHeight="200" MaxHeight="250">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Ad Soyad" Width="130" DisplayMemberBinding="{Binding Name}"/>
                                        <GridViewColumn Header="E-Posta Adresi" Width="170" DisplayMemberBinding="{Binding EmailAddress}"/>
                                        <GridViewColumn Header="İşlem" Width="70">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Sil" Tag="{Binding Id}" Click="BtnDeleteEmail_Click" Background="#E74C3C" Foreground="White" Padding="8,2" BorderThickness="0">
                                                        <Button.Resources>
                                                            <Style TargetType="Border">
                                                                <Setter Property="CornerRadius" Value="4"/>
                                                            </Style>
                                                        </Button.Resources>
                                                    </Button>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </Grid>
                    </Border>

                    <Border Background="{DynamicResource InputBackground}" CornerRadius="12" Padding="20">
                        <StackPanel>
                            <TextBlock Text="🔑 Şifreyi Değiştir" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource AppText}" Margin="0,0,0,15"/>

                            <TextBlock Text="Mevcut Şifre:" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                            <PasswordBox x:Name="TxtCurrentPassword" Padding="8" Margin="0,0,0,10" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}"/>

                            <TextBlock Text="Yeni Şifre:" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                            <PasswordBox x:Name="TxtNewPassword" Padding="8" Margin="0,0,0,10" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}"/>

                            <TextBlock Text="Yeni Şifre (Tekrar):" Foreground="{DynamicResource AppText}" Margin="0,0,0,5"/>
                            <PasswordBox x:Name="TxtConfirmPassword" Padding="8" Margin="0,0,0,15" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource AppText}"/>

                            <Button x:Name="BtnChangePassword" Content="🔑 Şifreyi Güncelle" Click="BtnChangePassword_Click" Height="40" Background="#FF4E6DE8" Foreground="White" BorderThickness="0">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>