<UserControl x:Class="ITMonitor.View.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             Background="Transparent"
             Loaded="UserControl_Loaded">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="5,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="📊 Sistem Özeti" Foreground="{DynamicResource AppText}" FontSize="24" FontWeight="SemiBold" FontFamily="Segoe UI" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <ProgressBar x:Name="ScanProgressBar" Width="100" Height="6" Margin="0,0,15,0" IsIndeterminate="True" Visibility="Hidden" Foreground="#007AFF" BorderThickness="0" Background="Transparent"/>
                <Button x:Name="BtnStartScan" Click="BtnStartScan_Click" Content="🚀 Taramayı Başlat" Height="35" Padding="15,0" Background="#007AFF" Foreground="White" FontWeight="SemiBold" FontSize="13" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource InputBackground}" CornerRadius="12" Margin="5,0,10,15" Padding="20,15">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Toplam Cihaz" Foreground="{DynamicResource AppText}" Opacity="0.7" FontSize="13" FontWeight="Medium"/>
                    <TextBlock x:Name="TxtTotalDevice" Text="0" Foreground="{DynamicResource AppText}" FontSize="36" FontWeight="Bold" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{DynamicResource InputBackground}" CornerRadius="12" Margin="5,0,10,15" Padding="20,15">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Aktif Sistemler" Foreground="{DynamicResource AppText}" Opacity="0.7" FontSize="13" FontWeight="Medium"/>
                    <TextBlock x:Name="TxtActiveDevice" Text="0" Foreground="#2ECC71" FontSize="36" FontWeight="Bold" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{DynamicResource InputBackground}" CornerRadius="12" Margin="5,0,10,15" Padding="20,15">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Hatalı Bağlantı" Foreground="{DynamicResource AppText}" Opacity="0.7" FontSize="13" FontWeight="Medium"/>
                    <TextBlock x:Name="TxtOfflineDevice" Text="0" Foreground="#E74C3C" FontSize="36" FontWeight="Bold" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Background="{DynamicResource InputBackground}" CornerRadius="12" Margin="5,0,5,15" Padding="20,15">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Son Tarama" Foreground="{DynamicResource AppText}" Opacity="0.7" FontSize="13" FontWeight="Medium"/>
                    <TextBlock x:Name="TxtLastScan" Text="-" Foreground="{DynamicResource AppText}" FontSize="32" FontWeight="Bold" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="5,0,10,5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource InputBackground}" CornerRadius="12" Margin="0,0,0,10" Padding="15">
                    <Grid>
                        <TextBlock Text="📈 Ortalama Ağ Gecikme Trendi (ms)" Foreground="{DynamicResource AppText}" Opacity="0.8" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Top"/>
                        <lvc:CartesianChart x:Name="PingLineChart" Margin="0,25,0,0" LegendLocation="None">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Zaman" Labels="{Binding ChartTimeLabels}" Foreground="{DynamicResource AppText}"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Gecikme (ms)" Foreground="{DynamicResource AppText}"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Background="{DynamicResource InputBackground}" CornerRadius="12" Margin="0,5,0,0" Padding="15">
                    <Grid>
                        <TextBlock Text="🍩 Ağ Sağlık Dağılımı" Foreground="{DynamicResource AppText}" Opacity="0.8" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Top"/>
                        <lvc:PieChart x:Name="HealthPieChart" Margin="0,20,0,0" LegendLocation="Right" InnerRadius="40"/>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Column="1" Background="{DynamicResource InputBackground}" CornerRadius="12" Margin="5,0,5,5" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="120"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="⚠️ Kritik / Sorunlu Cihazlar" Foreground="#E74C3C" FontSize="15" FontWeight="Bold" Margin="0,0,0,10"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
                        <ItemsControl x:Name="CriticalDevicesControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#2A1A1A" BorderBrush="#E74C3C" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="❌" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" Foreground="{DynamicResource AppText}" FontSize="13" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding IpOrUrl}" Foreground="{DynamicResource AppText}" Opacity="0.6" FontSize="11"/>
                                                <TextBlock Foreground="#E74C3C" FontSize="11" FontWeight="SemiBold" Margin="0,2,0,0">
                                                    <Run Text="Hata: "/>
                                                    <Run Text="{Binding LastErrorCode}"/>
                                                </TextBlock>
                                            </StackPanel>

                                            <TextBlock Grid.Column="2" Text="{Binding LastScanTime, StringFormat='HH:mm'}" Foreground="{DynamicResource AppText}" Opacity="0.5" FontSize="11" VerticalAlignment="Top"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="⏱️ Canlı Loglar" Foreground="{DynamicResource AppText}" Opacity="0.7" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Border Grid.Row="1" Background="{DynamicResource AppBackground}" CornerRadius="6" Padding="5" BorderBrush="{DynamicResource AppBorder}" BorderThickness="1">
                            <ListBox x:Name="LogList" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource AppText}" FontSize="11" Margin="0,-6,0,0"/>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>